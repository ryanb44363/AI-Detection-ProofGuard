{
  "name": "proofguard-heroku-build",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "heroku-prebuild": "npm ci --prefix frontend",
    "heroku-postbuild": "VITE_API_URL=/api npm run build --prefix frontend && rm -rf backend/static && mkdir -p backend/static && cp -R frontend/dist/* backend/static/"
  }
}
